# STT service config
STT_URL=http://127.0.0.1:5007
STT_PORT=5007
STT_MODEL=small
STT_DEVICE=auto          # auto|cuda|cpu
STT_COMPUTE_TYPE=        # optional override

# App config (existing keys remain)
OPENAI_API_KEY=
